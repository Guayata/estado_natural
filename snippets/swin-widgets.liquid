{% if customer %}
                                    {% assign customerId = customer.id %}
                                    {% assign customerName = customer.name %}
                                    {% assign customerEmail = customer.email %}
                                {% endif %}
                                <script>AMSWINConfig = {user : {name : "{{ customerName }}", email : "{{ customerEmail }}", id : "{{ customerId }}", country : ""}, site : {id : "68803", domain : "estado-natural.myshopify.com", platform : "sw_shopify"}};</script>
                                